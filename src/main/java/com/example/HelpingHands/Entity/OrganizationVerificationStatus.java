package com.example.HelpingHands.Entity;

/** Moderation status for an organization account. Set by admins (see admin panel roadmap item). */
public enum OrganizationVerificationStatus {
    PENDING,
    VERIFIED,
    REJECTED
}
